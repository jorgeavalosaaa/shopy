// Lo que me imagino es crear funciones para cada funcionalidad que hay en la página, para no repetir nada.

// Me imagino un main en el que solo recolecto los objetos, aunque supongo que eso es para cada layout.

// Creo que puedo tener un main que llame al resto, o quizás no.

// Debería de usar MVC en el front-end? ¿Hay alguna arquitectura?

// Qué pasa si quiero consumir alguna API?

// Hay alguna forma en la que pueda usar objetos? No lo sé.

// Si llego a utilizar una API supongo que sería para las compras, aunque quizá eso se hace en el back-end. ¿Cómo se llama una API en el back-end? Creo que ya lo he hecho.
